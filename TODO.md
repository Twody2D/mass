# TODO

Задачи проекта. Claude **не** берёт их в работу по своей инициативе — только по команде владельца.

## Vertical slice 1 — остров и 10 000 человечков

Цель: открыть проект → большой процедурный остров → 10 000 разноцветных человечков ходят сами →
свободная камера → HUD. **После достижения — остановиться.**

- [x] `chore(project)` — скелет Godot-проекта, `.gitignore`, документация, CLAUDE.md
- [x] `feat(core)` — `GameConfig`: `BOT_COUNT`, `MAP_SEED`, частоты тиков, размер карты, команды
- [x] `feat(world)` — процедурный остров: купол + рельеф → heightmap → меш;
      API `get_height`, `is_land`, `random_land_point`
- [x] `feat(camera)` — свободная летающая камера: WASD + мышь, Shift-ускорение, скорость колесом
- [x] `feat(simulation)` — модель данных ботов (SoA) и спавн по seed на суше
- [x] `feat(simulation)` — фиксированный тик, движение, бакетный AI, привязка к высоте,
      удержание в границах острова
- [x] `feat(rendering)` — `MultiMeshInstance3D` crowd renderer, заливка через `multimesh.buffer`,
      цвет по команде
- [x] `feat(ui)` — debug HUD: Alive / Total / FPS / Sim tick / Sim time / Sim speed / Seed
      + pause, resume, restart, изменение скорости
- [x] `perf(simulation)` — замеры масштабирования 100 → 1 000 → 5 000 → 10 000, правки по итогам
- [x] `docs(architecture)` — синхронизация документации с реальным кодом

**Срез закрыт.** Дальше — только по команде владельца.

## Найдено по ходу

- Задать **точный** seed можно только через `game_config.gd`; `N` даёт случайный. Текстовое поле
  дралось бы с клавиатурным управлением, поэтому текущий seed просто всегда виден на экране.
- Godot с `--headless` **не завершается**, если скрипт сцены не парсится, — процесс висит молча.
  Инструменты запускать с `--quit-after`, это снимает вопрос.

## Открытые вопросы среза

- Тик занимает 17.7% бюджета на 10 000 ботов, полный кадр — 73.9 FPS. Запас есть; оптимизировать
  нечего, пока не появятся события.
- Около 0.05% ботов оказывается над водой: прямая к близкой цели изредка срезает залив. Заметно
  не будет, но если понадобится — лечится проверкой пути, а не увеличением числа попыток.

- Достаточно ли 20 Гц тика визуально, или нужна интерполяция между тиками? Решается замером
  после реализации рендера, не раньше.
- Разрешение heightmap: 256×256 даёт 31% суши и генерацию за ~25 мс; поднимать только если
  силуэт берега окажется слишком грубым вблизи.
- Береговая линия всё ещё довольно округлая. Улучшать только если это будет заметно на видео.
- Кромка воды заметно ступенчатая вблизи: клетка heightmap — 4 м. Ждём ботов на карте, возможно
  вопрос отпадёт сам.

## После среза — не начинать без команды

- LOD для толпы (near / medium / far / very far)
- Spatial grid — как только появится первое взаимодействие ботов
- Граф регионов и навигация по нему
- `EventManager` и первые события: Meteor, Flood, Shrinking Safe Zone, Mass Supply Drop, Team War
- Cinematic Director
- Выбор конкретного бота и слежение за ним камерой
- Расширение HUD: leaderboard, статистика команд, лента событий, миникарта
- Тесты архитектурных инвариантов: переходы состояний, spatial-запросы, детерминизм seed
